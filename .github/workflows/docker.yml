name: Docker

on:
  # Defensive weekly rebuild of the 2 newest versions (new jars normally arrive
  # as a spigot-build flake-input bump — see push below).
  schedule:
    - cron: '30 9 * * 1' # Mondays 09:30 UTC

  # Manual builds. Selector mirrors spigot-build: 'latest', 'all', 'missing'
  # (not-yet-pushed + newest N), or a specific version.
  workflow_dispatch:
    inputs:
      version:
        description: "Images to build: 'latest', 'all', 'missing', or a specific version (e.g. 26.1.2)"
        required: true
        default: "latest"
      newest_n:
        description: "In 'missing' mode, how many newest versions to always rebuild"
        required: false
        default: "2"

  # Image source OR the pinned jar set changed → rebuild. flake.lock covers the
  # Dependabot `nix flake update spigot-build` bump that brings in new jars.
  push:
    branches:
      - master
    paths:
      - .github/workflows/docker.yml
      - src/**
      - flake.nix
      - flake.lock

  pull_request:

permissions:
  contents: read

concurrency:
  group: docker-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # Which images to build. The version list + per-version JDK come from the
  # PINNED spigot-build input via `.#imageMatrix`, so they match exactly what
  # `nix build .#"<ver>"` will build.
  matrix:
    runs-on: ubuntu-latest
    name: Compute matrix
    outputs:
      docker_matrix: ${{ steps.compute.outputs.docker_matrix }}
      docker_build_matrix: ${{ steps.compute.outputs.docker_build_matrix }}
      docker_count: ${{ steps.compute.outputs.docker_count }}
      docker_build_count: ${{ steps.compute.outputs.docker_build_count }}
      arch_list: ${{ steps.compute.outputs.arch_list }}
      latest: ${{ steps.compute.outputs.latest }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            accept-flake-config = true

      - name: Compute matrix
        id: compute
        env:
          INPUT_VERSION: ${{ github.event.inputs.version }}
          NEWEST_N: ${{ github.event.inputs.newest_n || '2' }}
          IMAGE_NAME: ${{ vars.IMAGE_NAME }}
        run: |
          set -euo pipefail
          full="$(nix eval --json .#imageMatrix)"   # [{spigot, java}] from spigot-build
          event='${{ github.event_name }}'
          # Numeric version sort (strip any "-preN"/"-rcN" suffix first so a
          # pre-release tag doesn't break tonumber; it groups with its release).
          SORT='sort_by(.spigot | split("-")[0] | split(".") | map(tonumber))'
          latest="$(printf '%s' "$full" | jq -r "$SORT | last | .spigot // \"\"")"

          # Pick the selector per event. workflow_dispatch mirrors spigot-build
          # (latest | all | missing | <specific>); the schedule always rebuilds
          # the 2 newest (defensive); a PR smoke-builds the newest only.
          case "$event" in
            pull_request)      sel=latest ;;
            schedule)          sel=newest; NEWEST_N=2 ;;
            workflow_dispatch) sel="${INPUT_VERSION:-latest}" ;;
            *)                 sel=latest ;;
          esac

          case "$sel" in
            latest)
              docker_matrix="$(printf '%s' "$full" | jq -c "$SORT | [last]")" ;;
            newest)
              docker_matrix="$(printf '%s' "$full" | jq -c --argjson n "$NEWEST_N" "[limit(\$n; ($SORT | reverse | .[]))]")" ;;
            all)
              docker_matrix="$full" ;;
            missing)
              # Already published = tags on Docker Hub; (re)build the rest plus
              # the newest N as a safety net.
              have="$(bash .github/scripts/dockerhub-tags.sh "$IMAGE_NAME")"
              docker_matrix="$(printf '%s' "$full" | jq -c --argjson have "$have" --argjson n "$NEWEST_N" \
                "( $SORT ) as \$s | ( [ \$s[] | select(.spigot | IN(\$have[]) | not) ] + [ limit(\$n; (\$s | reverse | .[])) ] ) | unique_by(.spigot) | $SORT")" ;;
            *)
              docker_matrix="$(printf '%s' "$full" | jq -c --arg v "$sel" '[.[] | select(.spigot == $v)]')" ;;
          esac

          # Multi-arch: expand version → (version × arch) on native runners.
          arch_meta='{"amd64":{"runner":"ubuntu-24.04","nix_system":"x86_64-linux"},"arm64":{"runner":"ubuntu-24.04-arm","nix_system":"aarch64-linux"}}'
          docker_build_matrix="$(jq -nc --argjson v "$docker_matrix" --argjson meta "$arch_meta" \
            '[ $v[] as $ver | ($meta | keys[]) as $arch | $ver + {arch: $arch} + $meta[$arch] ]')"
          arch_list="$(printf '%s' "$arch_meta" | jq -r 'keys | join(" ")')"

          {
            echo "docker_matrix=$docker_matrix"
            echo "docker_build_matrix=$docker_build_matrix"
            echo "docker_count=$(printf '%s' "$docker_matrix" | jq 'length')"
            echo "docker_build_count=$(printf '%s' "$docker_build_matrix" | jq 'length')"
            echo "arch_list=$arch_list"
            echo "latest=$latest"
          } >> "$GITHUB_OUTPUT"
          echo "images: $docker_build_matrix"
          echo "latest: $latest"

  # Build each image with Nix (the flake bakes the version-correct JRE + the
  # pinned spigot.jar from spigot-build). streamLayeredImage is single-arch, so
  # build one tarball per (version × arch) on a native runner; the next job
  # stitches them into a multi-arch manifest. PR events build but never push.
  docker-build:
    needs: matrix
    if: needs.matrix.outputs.docker_build_count != '0'
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 60

    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.matrix.outputs.docker_build_matrix) }}

    name: Build spigot:${{ matrix.spigot }} (${{ matrix.arch }})

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            accept-flake-config = true

      - name: Cache Nix store
        uses: nix-community/cache-nix-action@v7
        with:
          primary-key: nix-${{ runner.os }}-${{ matrix.arch }}-${{ hashFiles('**/flake.lock', '**/flake.nix', 'src/entrypoint.sh') }}
          restore-prefixes-first-match: nix-${{ runner.os }}-${{ matrix.arch }}-
          gc-max-store-size-linux: 5G
          purge: true
          purge-prefixes: nix-${{ runner.os }}-${{ matrix.arch }}-
          purge-created: 0
          purge-last-accessed: P7D
          purge-primary-key: never

      - name: Extract Docker labels
        uses: docker/metadata-action@v6
        id: meta
        with:
          images: ${{ vars.IMAGE_NAME }}
          tags: type=raw,value=${{ matrix.spigot }}
          labels: |
            org.opencontainers.image.authors=D3strukt0r <dev@d3strukt0r.dev>
            org.opencontainers.image.title=Spigot ${{ matrix.spigot }}
            org.opencontainers.image.description=Spigot Minecraft server ${{ matrix.spigot }} (JDK ${{ matrix.java }})

      - name: Build OCI image with Nix
        env:
          LABELS_RAW: ${{ steps.meta.outputs.labels }}
        run: |
          set -euo pipefail
          # metadata-action emits "KEY=VAL\n…"; the flake reads it via fromJSON.
          # --impure feeds ONLY the labels; the jar + version are pure inputs (the
          # jar is the pinned spigot-build fetch, the version is the attr path).
          export DOCKER_LABELS_JSON=$(jq -Rsc '
            split("\n") | map(select(length > 0))
            | map(split("=") | { key: .[0], value: (.[1:] | join("=")) })
            | from_entries
          ' <<<"$LABELS_RAW")
          nix build --impure --print-build-logs ".#\"${{ matrix.spigot }}\""
          cp -L result image.tar

      - name: Generate SBOM (per-arch)
        run: nix run nixpkgs#syft -- docker-archive:image.tar -o spdx-json=sbom.json

      - name: Upload image + SBOM
        uses: actions/upload-artifact@v7
        with:
          name: image-${{ matrix.spigot }}-${{ matrix.arch }}
          path: |
            image.tar
            sbom.json
          retention-days: 1
          if-no-files-found: error

  # Per version: stitch the per-arch tarballs into a multi-arch manifest, push it
  # (tagged with the MC version, plus :latest for the newest), and sign + attest
  # the SBOMs. Skipped on PRs.
  docker-manifest:
    needs: [matrix, docker-build]
    if: github.event_name != 'pull_request' && needs.matrix.outputs.docker_count != '0'
    permissions:
      contents: read
      id-token: write
      packages: write
    runs-on: ubuntu-24.04

    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.matrix.outputs.docker_matrix) }}

    name: Manifest spigot:${{ matrix.spigot }}

    services:
      registry:
        image: registry:3
        ports:
          - 5000:5000

    env:
      dockerhub_token: ${{ secrets.DOCKERHUB_TOKEN }}
      ARCH_LIST: ${{ needs.matrix.outputs.arch_list }}

    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
        with:
          buildkitd-config-inline: |
            [registry."localhost:5000"]
              http = true

      - name: Wait for local registry
        run: |
          for _ in $(seq 1 30); do
            curl -sf http://localhost:5000/v2/ >/dev/null && exit 0
            sleep 1
          done
          echo "registry never came up"; exit 1

      - name: Install skopeo
        run: sudo apt-get update -y && sudo apt-get install -y --no-install-recommends skopeo

      - name: Log in to Docker Hub
        if: vars.DOCKERHUB_USERNAME != '' && env.dockerhub_token != ''
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Install cosign
        uses: sigstore/cosign-installer@v4.1.2

      - name: Download this version's per-arch artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: image-${{ matrix.spigot }}-*
          path: artifacts

      - name: Stage per-arch tarballs into the local registry
        id: stage
        run: |
          set -euo pipefail
          DIGESTS='{}'
          for arch in $ARCH_LIST; do
            archive="artifacts/image-${{ matrix.spigot }}-${arch}/image.tar"
            test -f "$archive" || { echo "missing $archive"; exit 1; }
            skopeo copy --dest-tls-verify=false \
              docker-archive:"$archive" \
              docker://localhost:5000/staging:${{ matrix.spigot }}-${arch}
            d=$(skopeo inspect --tls-verify=false \
                  docker://localhost:5000/staging:${{ matrix.spigot }}-${arch} \
                  --format '{{.Digest}}')
            DIGESTS=$(jq -nc --argjson cur "$DIGESTS" --arg a "$arch" --arg d "$d" '$cur + {($a): $d}')
            echo "Staged ${arch} → ${d}"
          done
          echo "digests=$DIGESTS" >> "$GITHUB_OUTPUT"
          jq -nc --argjson d "$DIGESTS" '$d' > "digests-${{ matrix.spigot }}.json"

      - name: Upload digest map
        uses: actions/upload-artifact@v7
        with:
          name: digests-${{ matrix.spigot }}
          path: digests-${{ matrix.spigot }}.json
          retention-days: 1
          if-no-files-found: error

      - name: Resolve tags
        uses: docker/metadata-action@v6
        id: meta
        with:
          images: ${{ vars.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ matrix.spigot }}
            type=raw,value=latest,enable=${{ matrix.spigot == needs.matrix.outputs.latest }}

      - name: Create + push manifest list
        if: vars.DOCKERHUB_USERNAME != '' && env.dockerhub_token != ''
        env:
          TAGS: ${{ steps.meta.outputs.tags }}
          ANNOTATIONS: ${{ steps.meta.outputs.annotations }}
          DIGESTS: ${{ steps.stage.outputs.digests }}
        run: |
          set -euo pipefail
          ANN_FLAGS=()
          while IFS= read -r ann; do [ -z "$ann" ] && continue; ANN_FLAGS+=("--annotation" "$ann"); done <<< "$ANNOTATIONS"
          TAG_FLAGS=()
          while IFS= read -r tag; do [ -z "$tag" ] && continue; TAG_FLAGS+=("-t" "$tag"); done <<< "$TAGS"
          SRC_REFS=()
          for arch in $ARCH_LIST; do
            d=$(jq -r --arg a "$arch" '.[$a]' <<<"$DIGESTS")
            SRC_REFS+=("localhost:5000/staging@${d}")
          done
          docker buildx imagetools create "${ANN_FLAGS[@]}" "${TAG_FLAGS[@]}" "${SRC_REFS[@]}"

      - name: Sign per-arch images + attach SBOMs
        if: vars.DOCKERHUB_USERNAME != '' && env.dockerhub_token != ''
        env:
          IMAGE_NAME: ${{ vars.IMAGE_NAME }}
          DIGESTS: ${{ steps.stage.outputs.digests }}
        run: |
          set -euo pipefail
          for arch in $ARCH_LIST; do
            d=$(jq -r --arg a "$arch" '.[$a]' <<<"$DIGESTS")
            cosign sign --yes "${IMAGE_NAME}@${d}"
            cosign attest --yes --predicate "artifacts/image-${{ matrix.spigot }}-${arch}/sbom.json" \
              --type spdxjson "${IMAGE_NAME}@${d}"
          done

      - name: Sign the manifest list
        if: vars.DOCKERHUB_USERNAME != '' && env.dockerhub_token != ''
        env:
          TAGS: ${{ steps.meta.outputs.tags }}
        run: |
          set -euo pipefail
          while IFS= read -r tag; do
            [ -z "$tag" ] && continue
            DIGEST=$(docker buildx imagetools inspect "$tag" --format '{{json .Manifest}}' | jq -r .digest)
            cosign sign --yes "${tag}@${DIGEST}"
          done <<< "$TAGS"

  # SLSA build provenance per published (version × arch) digest.
  docker-attest:
    needs: [matrix, docker-manifest]
    if: github.event_name != 'pull_request' && needs.matrix.outputs.docker_build_count != '0'
    permissions:
      contents: read
      id-token: write
      attestations: write
      packages: write
    runs-on: ubuntu-24.04

    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.matrix.outputs.docker_build_matrix) }}

    name: Attest spigot:${{ matrix.spigot }} (${{ matrix.arch }})

    env:
      dockerhub_token: ${{ secrets.DOCKERHUB_TOKEN }}

    steps:
      - name: Log in to Docker Hub
        if: vars.DOCKERHUB_USERNAME != '' && env.dockerhub_token != ''
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Download digest map
        uses: actions/download-artifact@v8
        with:
          name: digests-${{ matrix.spigot }}
          path: .

      - name: Resolve this arch's digest
        id: d
        run: |
          set -euo pipefail
          digest=$(jq -r --arg a "${{ matrix.arch }}" '.[$a]' "digests-${{ matrix.spigot }}.json")
          echo "digest=$digest" >> "$GITHUB_OUTPUT"

      - name: Attest SLSA provenance
        if: vars.DOCKERHUB_USERNAME != '' && env.dockerhub_token != ''
        uses: actions/attest@v4
        with:
          subject-name: docker.io/${{ vars.IMAGE_NAME }}
          subject-digest: ${{ steps.d.outputs.digest }}
          push-to-registry: true

  # Record which commit produced each published image: one git tag per built
  # version, on the build commit, force-moved when the version is rebuilt on a
  # newer commit. The registry is the source of truth for "what's published"
  # (see the matrix's `missing` selector); these tags are for provenance.
  tag:
    needs: [matrix, docker-manifest]
    if: github.event_name != 'pull_request' && needs.matrix.outputs.docker_count != '0' && vars.DOCKERHUB_USERNAME != ''
    permissions:
      contents: write
    runs-on: ubuntu-latest
    name: Tag build commit

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Tag built versions on this commit
        env:
          MATRIX: ${{ needs.matrix.outputs.docker_matrix }}
        run: |
          set -euo pipefail
          mapfile -t versions < <(printf '%s' "$MATRIX" | jq -r '.[].spigot')
          [ "${#versions[@]}" -gt 0 ] || { echo "no versions to tag"; exit 0; }
          for v in "${versions[@]}"; do
            git tag -f "$v" "$GITHUB_SHA"
          done
          # Distinct tag refs → a single force push moves any that were built on
          # an older commit. Tag pushes via GITHUB_TOKEN don't re-trigger CI.
          git push -f origin "${versions[@]}"
          echo "Tagged ${GITHUB_SHA} with: ${versions[*]}"
